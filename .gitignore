# Local secrets for Docker Compose substitution (see .env.example)
.env

# Runtime caches (HF hub, torch, XDG) — stored under Tools/LLM via docker-compose env
.docker-cache/
.cache/
.pytest_cache/
*.gguf

# Unsloth auto-generated trainer compilation cache (regenerated on each training run)
unsloth_compiled_cache/

# Generated workflow trace reports
reports/workflow_runs/
