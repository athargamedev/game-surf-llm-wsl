interface:
  display_name: "NPC Model Tuning"
  short_description: "Tune and train local NPC LLMs in WSL"
  default_prompt: "Use $npc-model-tuning to test the local LM Studio model, tune an NPC profile, and run the WSL training pipeline."

policy:
  allow_implicit_invocation: true
