interface:
  display_name: "NPC Model Tuning"
  short_description: "Train and validate NPC LoRAs inside WSL"
  default_prompt: "Use $npc-model-tuning to verify WSL CUDA readiness, train an NPC LoRA with Unsloth, and trace runtime validation."

policy:
  allow_implicit_invocation: true
